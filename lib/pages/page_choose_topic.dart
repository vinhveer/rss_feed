import 'package:flutter/material.dart';

class PageChooseTopic extends StatelessWidget {
  const PageChooseTopic({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Test"),
      ),
      body: Container(
        padding: EdgeInsets.all(15),
      ),
    );
  }
}
