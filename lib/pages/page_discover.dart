import 'package:flutter/material.dart';

class PageDiscover extends StatelessWidget {
  const PageDiscover({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
